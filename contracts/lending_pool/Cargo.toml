[package]
name = "stellar-lend-lending-pool"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { workspace = true }
soroban-token-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
ed25519-dalek = { workspace = true }
stellar-lend-token = { path = "../token" }
stellar-lend-price-oracle = { path = "../price_oracle" }
